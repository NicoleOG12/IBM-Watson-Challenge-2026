You are a senior backend engineer.

Create a Python backend using FastAPI that will serve as the orchestration layer for an AI-powered data copilot.

Requirements:
- Create a clean project structure (controllers, services, models)
- Implement a POST /query endpoint that receives:
  - user_id
  - natural_language_query
- Implement a GET /health endpoint
- Use environment variables for configuration
- Prepare the project for future integration with LLMs and databases

Return:
- Folder structure
- Main files (app.py, routes, services)
- Example request/response