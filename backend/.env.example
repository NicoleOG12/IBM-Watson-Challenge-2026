# Application
APP_NAME=AI Data Copilot
APP_VERSION=0.1.0
ENVIRONMENT=development
DEBUG=True

# Server
HOST=0.0.0.0
PORT=8000

# IBM watsonx.ai
WATSONX_API_KEY=your-ibm-cloud-api-key
WATSONX_PROJECT_ID=your-watsonx-project-id
WATSONX_URL=https://us-south.ml.cloud.ibm.com
WATSONX_MODEL_ID=ibm/granite-13b-chat-v2
WATSONX_MAX_NEW_TOKENS=512
WATSONX_TEMPERATURE=0.0
# Set to False when real credentials are provided
WATSONX_MOCK=True

# IBM Consulting Advantage (ICA)
# Developer keys: ICA UI → Settings → API Keys → ICA APIs
ICA_KEY=your-ica-developer-api-key
ICA_BASE_URL=https://api.nextgen-beta.ica.ibm.com/ica/v1
# Chat model to use for NL→SQL via /chat-models/chat/completions
# Options seen in your account: meta-llama/llama-4-maverick-17b-128e-instruct-fp8 | claude-sonnet-4-5 | gpt-5.1-chat-gus
ICA_MODEL_ID=meta-llama/llama-4-maverick-17b-128e-instruct-fp8
# Set to False to route NL→SQL through the real ICA API
ICA_MOCK=True

# Database
DATABASE_URL=postgresql://user:password@localhost:5432/copilot
# Set to False when a real database connector is registered
DB_MOCK=True

<<<<<<< HEAD
# ── AWS ─────────────────────────────────────────────────────────────────────
# Region
AWS_REGION=sa-east-1

# IAM Role (used by the Lambda execution environment — boto3 picks it up
# automatically when running inside Lambda; no extra config needed).
# When running OUTSIDE Lambda (local dev / EC2 without instance profile) you
# can either set AWS_ACCESS_KEY_ID + AWS_SECRET_ACCESS_KEY, or set
# AWS_ROLE_ARN and the app will call STS AssumeRole at startup.
AWS_ROLE_ARN=arn:aws:iam::ACCOUNT_ID:role/ConsulteFunction02-role-3cnsnkm

# ── Athena ───────────────────────────────────────────────────────────────────
# Set USE_ATHENA=True to route queries through real AWS Athena
USE_ATHENA=False
ATHENA_DB=db_watson
ATHENA_OUTPUT=s3://database-watson/query-results/

# ── Lambda ───────────────────────────────────────────────────────────────────
# Name or ARN of the Lambda function to invoke for async / ETL triggers
AWS_LAMBDA_FUNCTION_NAME=ConsulteFunction02

# ── S3 ───────────────────────────────────────────────────────────────────────
# Bucket used for raw data uploads / exports (separate from Athena results)
AWS_S3_DATA_BUCKET=database-watson

# ── Glue ─────────────────────────────────────────────────────────────────────
# Glue Data Catalog database (shared with Athena)
GLUE_DATABASE=db_watson
# Glue ETL job name (optional — leave empty if you don't trigger ETL jobs)
GLUE_JOB_NAME=
=======
# AWS Athena (production query execution)
# Set USE_ATHENA=True to route queries through real AWS Athena
USE_ATHENA=False
ATHENA_DB=default
ATHENA_OUTPUT=s3://your-bucket/athena-results/
AWS_REGION=us-east-1
>>>>>>> a749ebc84c4475b1a91e44c8818945562ebe6f32

# Conversation memory
MEMORY_MAX_HISTORY=10

# Anomaly detection thresholds
# Coefficient of variation (%) above which a column is flagged as highly variable
ANOMALY_VARIATION_THRESHOLD=30.0
# IQR fence multiplier for outlier detection (standard Tukey = 1.5; lower = stricter)
ANOMALY_IQR_MULTIPLIER=1.5

# Audit logging
AUDIT_ENABLED=True
AUDIT_LOG_FILE=logs/audit.log

# Vector Store (Pinecone)
PINECONE_API_KEY=
PINECONE_ENVIRONMENT=us-east-1-aws

# Security
SECRET_KEY=change-me-in-production
