{
  "name": "watson-challenge-2026",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run vercel-build",
    "start": "cd frontend && npm run start"
  }
}
